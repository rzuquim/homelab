all_nodes:
  children:
    sandbox:
      hosts:
        192.168.56.10:
    test:
      hosts:
        192.168.56.20:

  vars:
    ansible_user: ansible
    ansible_python_interpreter: /usr/bin/python3
